{
  "name": "BryApp Dev Container",
  "dockerFile": "../Dockerfile",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "extensions": ["ms-python.python", "dbaeumer.vscode-eslint"],
  "forwardPorts": [8000]  // Adjust the port number as per your application's requirements
}
