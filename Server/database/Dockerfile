# Use the official SQLite image as base image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /database

# Copy the database file into the container
COPY database.db /database/
